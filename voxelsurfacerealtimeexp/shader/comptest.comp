#version 430

uniform TestUBO
{
  float Result;
};

// output buffer
layout(binding = 0) writeonly buffer oBuffer
{
  float Result;
} Out;

// compute shader size
layout (local_size_x = 1) in;
void main()
{	
  Out.Result = Result;
}
